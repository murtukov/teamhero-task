import React from 'react';
import PropTypes from 'prop-types';

Row.propTypes = {

};

function Row({children}) {
    return (
        <div>
            {children}
        </div>
    );
}

export default Row;